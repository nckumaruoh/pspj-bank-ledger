# Week 10 verified trace

`500` parses and passes the positive-value rule. `-1` parses but fails the positive-value rule. `abc` throws `NumberFormatException`, which the helper translates into the custom validation message.
